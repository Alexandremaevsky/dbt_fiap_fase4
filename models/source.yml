version: 2

sources:
   - name: sources
     database: dw-fiap-fase4
     schema: fiap_fase4
     tables:
      - name: calendar
      - name: listings
      - name: neighbourhoods
      - name: reviews